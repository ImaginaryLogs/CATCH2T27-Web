import { initializeApp } from "firebase-admin/app"; // TODO: install firebase-admin to repo

export const app = initializeApp({ credential: applicationDefault() databaseURL: "TODO!!!!!!!!!!!!!!"});
